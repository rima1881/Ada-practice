with Ada.Text_IO;use Ada.Text_IO;
with Ada.Integer_Text_IO;use Ada.Integer_Text_IO;

procedure part4  is
    num : Integer;
    i : Integer;
begin
    

    Put("Enter a number");
    Get(num);

    Put(num rem 5);

    --while i < num/2 loop


   -- end loop;

end part4;